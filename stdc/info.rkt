#lang info

(define collection 'use-pkg-name)
(define pkg-desc "A sort of C/C++ utilities in pure Typed Racket")

(define version "1.0")
(define test-omit-paths 'all)

(define scribblings '(["tamer/stdc.scrbl" (main-doc) (parsing-library)]))
